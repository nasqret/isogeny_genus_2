# Computational Reconstruction

This document will become the human-readable master reconstruction of every
paper computation. Its generated evidence table is sourced from
`research/data/claims.json`.

The initial state records source locations, engines, execution locations, and
required certificates. Results are added only after independent execution.
